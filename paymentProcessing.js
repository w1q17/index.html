function buyNow(planName, price) {
    // тут могут быть вызовы к серверу для обработки покупки
    alert(`You have purchased the ${planName} for $${price}.`);
    // можно добавить логику для перенаправления пользователя на страницу подтверждения или обновления баланса
}
